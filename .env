# mock — использовать моковую "базу данных" в localStorage, без реального бэкенда
# real — ходить на настоящий бэкенд по VITE_API_BASE_URL
VITE_API_MODE=mock

# Используется только когда VITE_API_MODE=real
VITE_API_BASE_URL=/api
